name: lead_generation_app
description: Free lead generation dashboard — USA businesses with recent 1-star reviews.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.0
  equatable: ^2.0.7
  http: ^1.3.0
  url_launcher: ^6.3.1
  google_fonts: ^8.2.0
  lucide_icons_flutter: ^3.1.15
  go_router: ^17.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
